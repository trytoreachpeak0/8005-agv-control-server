@{
    # 真车载端 WPF + 真 slots-simulator。这条场景的判据分两半：一半是行程带（多停靠的
    # UpcomingStopPlanSnapshot 车辆侧收不收），一半是四个停靠依次装载的真 IO 闭环。前者合成对端
    # 也能跑，后者不能——而它们必须在同一次运行里，因为 2026-09-10 现场那次正是「行程带被拒」
    # 把整条会话锁死，装载一次都没开始。
    Onboard = 'Real'

    # 种子自带的 12（N1-3_N1-7）加这三个，凑成四个不同的取货停靠。必须在这里声明，不能由场景在
    # 运行时注入——准入策略绑定内容的那个坑，见 multi-demand-four-stops.setup.psd1。
    ExtraStations = @{
        '13' = 'N2-6'
        '14' = 'N3-4'
        '15' = 'N4-2'
    }
}
