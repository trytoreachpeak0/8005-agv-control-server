# 批次 4（control-server#75），规格 8.3 批次 4 场景⑤：指派版本冻结，导入新版本不停车生效。
#
# 版本 1 就是默认前置按这里导入的那一版：只给 N1-3 一行、指 FRONT。不用默认表，是因为默认表还会带上默认站点
# `N1-3_N1-7` 里的 N1-7，而场景最后要把版本 1 的内容原样再导入一次，内容越少越容易看清「原样」。
# 入库已批准八仓事实与绑车走默认前置，分组从库里读，不在这里写仓号。
@{
    AreaAssignments = @(
        @{ Area = 'N1-3'; DispatchZone = 'MAP-25-WIRE_TO_GATE'; SlotPosition = 'FRONT' }
    )
}
