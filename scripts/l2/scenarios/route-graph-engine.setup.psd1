# 开着路网引擎跑一趟正常装载。
#
# 引擎默认关，既有场景因此面对的仍是它出现之前的那台服务端。这条场景把它打开，让派车真的经
# 过可达性判据与成本排序器——只有这样，「引擎是派车链路的硬依赖」才是被跑出来的，不是被声明
# 出来的。
#
# 运行态预算给 45 秒而不是默认值以外的数：合成 RIoT 就在回环上，10 秒周期一次不会漏，45 秒
# 的预算容得下一次抖动而不至于把一轮派车判成陈旧。
@{
    RouteGraph = @{
        Enabled              = $true
        DesignStateTtl       = '00:10:00'
        RuntimeRefreshPeriod = '00:00:10'
        RuntimeStateMaxAge   = '00:00:45'
    }
}
