# 批次7-10（control-server#215）：REQ-0328 仅当前车不合格时释放改派。
#
# 两台车：主车先接下需求，第二台一开始是停用的（RIoT enable=false），好让「谁先接」不由代价决定——那样场景
# 就在证一张图的摆位，而不是改派。主车离开本图之后才把第二台启用，需求只可能落到它身上。
#
# 不开路网、不配分区参数：这条场景里没有途中追加，只有「一车一单」的受理、释放与再受理。
@{
    Fleet = @(
        @{ AgvId = 'AGV-L2-002'; VehicleKey = 'BROKERX-L2-0002' }
    )
}
