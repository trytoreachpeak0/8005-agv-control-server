# control-server#72：分区归属表是唯一的 AREA 执行白名单。
#
# 场景自己做「已批准八仓事实入库」和「分区归属表导入」，导入的表只含 N1-3 与一个 T 开头 AREA，
# 这是要证的前提本身。所以关掉编排器的两项默认前置（control-server#71 的边车键）：默认表会把默认站点
# 里的 N1-3、N1-7 都归进分区，N1-7 在场景导入之前就可能被受理，场景要证的东西就没了。
#
# #71 合入之前编排器不认这两个键、也不做默认前置，写在这里不改变今天的行为。
@{
    SlotModelPreseed = $false
    AreaAssignments  = $false
}
