### G3 FP-IS-03：出发前安全检查在答复之后因安全状态变化而过期。真车载端 WPF + 真 slots-simulator。
#
# 离站等待 40 秒（原 15 秒）：场景要在检查发出之前把去关卡的路线改成不可达，让服务端拿着安全答复却发不了车。
# 批次 5（control-server#79）起同一个值也是到站后的录入期限，而真装置上录入实测 4.7–5.4 秒，15 秒只剩十秒
# 余量。抬到 40 秒两侧都够：仍远小于等「拿着 SAFE 却被建单门挡住」那条判据的 90 秒预算。
@{
    Onboard                     = 'Real'
    StationDepartureWaitTimeout = '00:00:40'
}
