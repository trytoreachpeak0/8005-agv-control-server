# 批次5-30（control-server#86）：ADR-cross-0058 决策 4，装货中仓门未闭而站点期限到期。
#
# 站点期限压到 40 秒（出厂 5 分钟，ADR-cross-0055）。期限从到站起算（control-server#79），这 40 秒里要先走完
# 「到站 → UIA 录入并提交」（实测 4.7–5.4 秒）和「装货命令 → 车载端开锁、等操作员」，再在期限之前读一次
# 「门开着但还没到期、没有告警」。装置默认 30 秒也够，但 40 秒让期限前那一读不必和开锁抢时间。
# 上界来自期限之后那段：过期再晾 30 秒，全场景仍在几分钟内跑完。
@{
    # 真车载端 WPF + 真 slots-simulator。判据要的是车辆自己按 IO 读数算出、经安全投影报给服务端的
    # LOCK_NOT_CLOSED；合成对端没有这段逻辑，给它种子只是让服务端收到一个字符串。
    Onboard                     = 'Real'
    StationDepartureWaitTimeout = '00:00:40'
}
