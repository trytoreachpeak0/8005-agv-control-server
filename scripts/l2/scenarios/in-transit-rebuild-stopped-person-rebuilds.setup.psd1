### 自动重建停住之后人工重建一次（control-server#345，出口甲）。
#
# 单车、合成车载端。两处改动环境：
# - 打开故障人工清除入口。人工出口挂在同一个入口上（control-server#299），产品里它默认不挂，现场要明确打开；场景打开的是同一个开关。
# - 同车重建（control-server#318）的延迟由产品默认 30 秒调成 8 秒，省机时。REQ-0361 的窗口保持默认 10 分钟：场景里两次取消
#   相隔几十秒，落在窗口内，才会停住。
@{
    OwnOrderRebuildDelay = '00:00:08'
    VehicleFaultRecovery = $true
}
