### 命令面出口：三台车，其中一台的在途单被 RIoT 报成 FAILED。
#
# 用三台车而不是一台，是因为这条场景要同时证两件事：命令面对出事的那台车做对了，以及它**只**
# 对那台车做了事。单车下第二件事无从证起——只有一台车时，「只针对它」与「针对所有车」是同一句话。
#
# 车队名单与 `three-vehicle-exit` 逐字相同，故意的：出口证据里两条场景面对的是同一支车队，
# 差别只在注入了什么。
@{
    Fleet = @(
        @{ AgvId = 'AGV-L2-002'; VehicleKey = 'BROKERX-L2-0002' },
        @{ AgvId = 'AGV-L2-003'; VehicleKey = 'BROKERX-L2-0003' }
    )
}
