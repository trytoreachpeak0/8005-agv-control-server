# 批次 5（control-server#83）：扫码前取消。
#
# 派车用 control-server#71 的默认前置（仓位模型入库、绑定、默认分区归属表），这里不退出。站点离站期限拉到十分钟，
# 远超本场景时长：终结必须来自取消，而不是期限到了——两条出口走同一段收尾代码，默认值下分不清是谁关的。
@{
    StationDepartureWaitTimeout = '00:10:00'
}
