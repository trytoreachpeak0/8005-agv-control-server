### G3 FP-IS-01：接单到取货点，真车载端 WPF + 真 slots-simulator。
#
# 断言要读车载端自己的日志库，所以只能跑在真装置上；合成对端没有那张表。
#
# 到站期限 5 分钟：批次 5（control-server#79）起，同一个值也是「车到站后多久没人录入子批号，服务端就结束
# 本站、取消需求」。本场景刻意停在 AwaitingSublot，到站之后还要等快照被确认、读车载端日志库、判终态，
# 整条尾巴都在这段期限里跑；装置默认的 5 秒会在判据跑完之前把本站结掉，连带推掉一版计划。取产品默认值
# 5 分钟，本场景不装货，没有「等发车」那一侧的上限。
@{
    Onboard                     = 'Real'
    StationDepartureWaitTimeout = '00:05:00'
}
