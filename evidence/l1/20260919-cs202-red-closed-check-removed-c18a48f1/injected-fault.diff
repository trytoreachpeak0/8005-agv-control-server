diff --git a/src/ControlServer.Host/Transport/OnboardRecoveryCoordinator.cs b/src/ControlServer.Host/Transport/OnboardRecoveryCoordinator.cs
index da0c7acb..ba0fa305 100644
--- a/src/ControlServer.Host/Transport/OnboardRecoveryCoordinator.cs
+++ b/src/ControlServer.Host/Transport/OnboardRecoveryCoordinator.cs
@@ -831,7 +831,7 @@ public sealed class OnboardRecoveryCoordinator(
             // the same action (control-server#169). Authorizing then would set a CLOSED session back to EXECUTING
             // and send the command, and the compensation's result would find an open session and settle the
             // demand the next session is now handling (control-server#175). A CLOSED session is never reopened.
-            if (session.State == "CLOSED")
+            if (false && session.State == "CLOSED") // FAULT INJECTION cs#202, never pushed
             {
                 return Response(root, "LoadCompensationRejected", new
                 {
