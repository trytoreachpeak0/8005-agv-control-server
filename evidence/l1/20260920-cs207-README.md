# cs#207 L1 证据索引

批次7-02 是行为不变的预重构。证据分三类，全部在本地 `dotnet test`（xunit.v3，`--filter`）上取得，每个目录里有原始输出。

## 1. 改前基线（行为不变的依据）

`tests/ControlServer.Tests/ZeroChangePins/` 下 14 份期望值，是提交 `e403a002`（产品代码仍是 `fp/v2-impl@47ae7376`）上跑出的结果：
八条终结路径的库状态（七张表全部列＋出站消息确认时刻）与两个看板端点的输出。连跑两次一致；随测试输入或挂钟变化的 8 列只钉「有没有值」。
改动后（`ea9d10a0`）同一组测试逐条通过，期望文件未改。

## 2. 红：非锚需求的读取（改动前）

`20260920-cs207-red-lookup-sites/`：查找口已有、各处还没改走它时，两需求旅程下 3 条红。

## 3. 红：注入故障（改动后的代码上逐一注入，确认测试能抓住）

每个目录里 `injected.diff` 是注入的改动，`dotnet-test.txt` 是结果；跑的是 `Batch7DemandTerminationTests` 与 `Batch7DemandJourneyLookupTests`（22 条）。

| 目录（后缀 `-ea9d10a0`） | 注入 | 红 |
| --- | --- | --- |
| `...-m1-close-without-last-check` | 取货停靠终结不判「最后一条」，总是关旅程 | 5 条：终结一条即关旅程、重放提前关、FAILED／UNKNOWN 前置里旅程被提前关 |
| `...-m2-unload-releases-without-last-check` | 卸货结果不判「最后一条」，总是放租约 | 4 条：非末条卸货放了租约、并发用例（卸货方不再读判定，等待超时） |
| `...-m3-last-read-before-write-transaction` | 直接完成入口把「最后一条」挪到写事务之前读 | 只有并发用例红：`Expected: 2026-09-19T09:06:00` / `Actual: null`——两个写者都看到对方未终结，没人放租约 |
| `...-m4-orphan-check-reads-journey-rows` | 孤儿检查改回读旅程行 | 只有「有从属行的非锚需求不是孤儿」红 |
| `...-m5-lookup-reads-anchor-column` | 查找口改回读锚列 `JourneyRuntimes.DemandId` | 7 条：非锚需求找不到旅程 |

## 4. 每一处改过的读取都有单需求测试钉着

`20260920-cs207-site-mutations-ea9d10a0/summary.txt`：把每一处改走查找口的读取单独改成「找不到旅程」，跑相关的 9 个测试类（232 条），列出变红的现有测试；每处的注入改动在同目录 `<处>.diff`。完整控制台输出太大（约 700 KB）未入库，`summary.txt` 已逐条列出失败的测试名。14 处全部至少有一条现有测试变红。

并发用例 `TwoDemandsOfOneJourneyEndingAtOnceCloseItExactlyOnce` 在 `ea9d10a0` 上单独连跑 5 次，5 次通过。
