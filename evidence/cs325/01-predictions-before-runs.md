# cs#325 对照组预期（跑之前写下，2026-09-27）

三端（修复后）：control-server = 本票分支（基于 `241f445d`），onboard `b789c3d4`，simulator `fb5f7c59`。

| 判据 | 修复后三端 | 新服务端 + 旧车载端 `65c86508`（hmi#199 之前） | 旧服务端 `ba9cb2d4`（cs#323 之前）+ 新车载端 |
| --- | --- | --- | --- |
| SST-00 走代理 | 绿 | 绿 | 绿 |
| SST-01 到站要子批（前提） | 绿 | 绿 | 绿 |
| SST-02 期限收尾（前提） | 绿 | 绿 | 绿 |
| SST-03 不再要子批 | 绿 | **红**：收到空清单也不撤 `_currentEntryRequest` | **红**：不发收尾快照 |
| SST-04 不再给取消 | 绿 | **红**（推的：入口跟着录入请求/清单项走；若旧车载端按清单项判断可能绿） | **红** |
| SST-05 清单不挂这单 | 绿 | 推的：**绿**——旧车载端照样按快照替换清单内容，空清单到了列表就空 | **红** |
| SST-09 窗口前提 | 绿 | 绿 | 绿（本来就不清） |
| SST-06 两下不掉线 | 绿 | 绿 | 绿（推的：cs#324 之前答 ACTION_NOT_ALLOWED，不掉线；MVP 线掉线那个是 hmi#39，v2 已含） |
| SST-07 取消两下都 STALE | 绿 | 绿（服务端的事） | **红**：原因码是 `ACTION_NOT_ALLOWED_IN_STATE` |
| SST-08 迟到扫码 STALE 拒收 | 绿 | 推的：服务端这半绿；提示区显示 `WORKLIST_REVISION_STALE`——旧车载端有没有这个码的文案/显示不确定（hmi#199 才补文案），可能红在显示那一半 | **红**：cs#324 之前没人答 |
| SST-10 STALE 之后撤录入 | 绿 | **红**：hmi#199 之前收到 STALE 不撤 | **红**（没有 STALE 可收；30 秒后仍可录入） |

两组「各自红在预期判据」的最低要求：新服务端 + 旧车载端至少红在 SST-03、SST-10；旧服务端 + 新车载端至少红在 SST-03、SST-05、SST-07、SST-08。
