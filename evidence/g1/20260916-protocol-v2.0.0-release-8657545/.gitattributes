# release-approval.json is bound byte-for-byte by the protocol-v2.0.0 tag message and the GitHub Release
# asset SHA-256; line-ending normalisation would make a checkout hash to something else.
release-approval.json -text
