# 红探针：撤掉 cs#303 之后，后侧先追加的混挂站点场景该红在哪里（跑之前写下）

写于 2026-09-22，运行之前。批次 7 出口 control-server#220，调度放行时的要求：先写预期，跑完逐条对。

## 跑什么

- 服务端：本地 detached worktree `rig-b7-18-cs-no303` 的 `d259898d`（不推送）。它等于分支上 `b86d280a`（场景已改为后侧先追加）
  再把 `JourneyRuntimeEngine.cs`、`JourneyStopCursor.cs` 两个文件还原到 cs#303 合入之前（`c4b04bb2^1`），差异见 `00-diff-stat.txt`。
- 车载端 `ecdb3a0b`、模拟器 `fb5f7c59`，与正式运行相同。
- 场景 `real-onboard-mixed-side-one-stop` × 1。

## 预期

**只有 `L2-MSO-10` 红，红在「先前后后」。**

推理：没有 cs#303 时，卸货挑本停靠第一条已装货的需求，按加入旅程的先后（`AddedAt`）、再按 `DemandId` 排。
三条需求的加入顺序是甲（前侧，初始派车）→ 丙（后侧，先追加）→ 乙（前侧，后追加），所以关卡上的卸货顺序是
甲、丙、乙，也就是 `FRONT,REAR,FRONT`。`L2-MSO-10` 要求第一个 `REAR` 在最后一个 `FRONT` 之后，
还要求丙的卸货命令建在乙的卸货提交之后、采样里丙那一仓在乙那一仓之后才开。这三处都会反过来。

其余各条为什么不该红：

- `L2-MSO-01`～`08`：都在关卡之前（追加、12 号站清单、装货、持货）。cs#303 只改卸货挑选，装货仍由扫码顺序决定，场景仍先扫乙。
- `L2-MSO-09`：关卡清单每一行的侧标来自区域指派（`WorklistItemSide`），与卸货先后无关。
- `L2-MSO-11`：一次一扇仍然成立，卸货仍一次只下一条，只是顺序变了。
- `L2-MSO-12`：三条照样各卸一次、旅程走完。

## 判定

- 只有 `L2-MSO-10` 红：符合预期。
- 除它以外还有别的红：逐条解释。
- `L2-MSO-10` 没红：停下来报告调度，不换别的注入去试。
