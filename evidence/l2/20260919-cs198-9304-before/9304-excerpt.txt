# grep -c 'are still configured but are ignored' logs/control-server.out.log
1
# grep -n 'are still configured but are ignored' logs/control-server.out.log
19:[23:40:08 WRN] JourneyRuntime:gateStationId (�ؿ�) / gateStationRiotId (210) are still configured but are ignored since control-server#160: the WIRE_TO_GATE end station is the Map's binding in the task type station preset. Remove the two keys, and check that binding if the gate here is not the preset's.
# L2 result line
结论：**PASS**
