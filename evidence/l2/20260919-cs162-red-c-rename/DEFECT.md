# 红证据：缺陷版本 rename-is-no-change

注入的缺陷：按旧票 #54 的读法把同 id 只改名判为无变化（CatalogBindingChangeClassifier）。

缺陷提交 `125a4d90` 在本地临时分支 `tmp-cs162-red-rename-is-no-change` 上，基于 `d223b14e`，未推送、用后删除。
判定与失败原因见同目录 `SUMMARY.md` 与 `assertions.json`。
